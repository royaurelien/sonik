#!/bin/bash
set -e

# Reload user systemd units *only when a bus is available*
if su "$SUDO_USER" -c "systemctl --user daemon-reload" 2>/dev/null; then
    echo "systemd user reload OK"
else
    echo "Skipping systemd --user reload (no user session detected)"
fi

exit 0
